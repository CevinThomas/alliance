import React from "react";
import SideMenuBar from "../../components/sideMenu/sideMenu";

const SideMenu = () => {
    return (
        <div className={"side-menu"} id={"account-side-menu"}>
            <SideMenuBar/>
        </div>
    );
};

export default SideMenu;
