import React from "react";
import AllSpaces from "../containers/spaces/allSpaces";

const ViewSpaces = () => {
    return (
        <div id={"spaces"}>
            <AllSpaces/>
        </div>
    );
};

export default ViewSpaces;
